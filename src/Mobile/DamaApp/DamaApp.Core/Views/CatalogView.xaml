<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DamaApp.Core.Views.CatalogView"   
             xmlns:templates="clr-namespace:DamaApp.Core.Views.Templates;assembly=DamaApp.Core"
             xmlns:views="clr-namespace:DamaApp.Core.Views;assembly=DamaApp.Core"
		     xmlns:viewModelBase="clr-namespace:DamaApp.Core.ViewModels.Base;assembly=DamaApp.Core"
             xmlns:animations="clr-namespace:DamaApp.Core.Animations;assembly=DamaApp.Core"
             xmlns:triggers="clr-namespace:DamaApp.Core.Triggers;assembly=DamaApp.Core"
             xmlns:behaviors="clr-namespace:DamaApp.Core.Behaviors;assembly=DamaApp.Core"
			 viewModelBase:ViewModelLocator.AutoWireViewModel="true"
             Title="Catalog">
    <ContentPage.Resources>
        <ResourceDictionary>

            <Style x:Key="ProductsListStyle" 
               TargetType="{x:Type ListView}">
                <Setter Property="RowHeight"
                        Value="400" />
                <Setter Property="VerticalOptions"
                        Value="Center" />
                <Setter Property="Margin"
                        Value="0" />
            </Style>

            <Style x:Key="FilterLabelStyle" 
               TargetType="{x:Type Label}">
                <Setter Property="TextColor"
                        Value="{StaticResource WhiteColor}" />
                <Setter Property="HorizontalOptions"
                        Value="Center" />
                <Setter Property="VerticalOptions"
                        Value="Center" />
            </Style>

            <animations:StoryBoard 
                x:Key="ProductsAnimation"    
                Target="{x:Reference Products}">
                <animations:FadeInAnimation 
                    Direction="Up"
                    Duration="1500"
                    Delay="250"/>
            </animations:StoryBoard>

        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Triggers>
        <EventTrigger 
            Event="Appearing">
            <triggers:BeginAnimation   
                Animation="{StaticResource ProductsAnimation}" />
        </EventTrigger>
    </ContentPage.Triggers>
    <Grid  
        ColumnSpacing="0"
        RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!-- FILTERS -->
        <Grid
            BackgroundColor="{StaticResource LightGreenColor}">
            <Label 
                Text="FILTER"
                Style="{StaticResource FilterLabelStyle}"/>
            <Grid.GestureRecognizers>
                <TapGestureRecognizer 
                        Tapped="OnFilterChanged"
                        NumberOfTapsRequired="1" />
            </Grid.GestureRecognizers>
        </Grid>
        <!-- CATALOG -->
        <Grid  
            Grid.Row="1">
            <Grid 
                IsVisible="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}">
                <Label 
                    Text="NÃO FORAM ENCONTRADOS PRODUTOS"      
                    IsVisible="{Binding Products.Count, Converter={StaticResource InverseCountToBoolConverter}}"
                    HorizontalOptions="Center"
                    VerticalOptions="Center"/>
            </Grid>
            <ListView 
                x:Name="Products"       
                IsVisible="{Binding Products.Count, Converter={StaticResource CountToBoolConverter}}"
                ItemsSource="{Binding TupleProducts}"
                HasUnevenRows="True"
                SeparatorVisibility="None"
                CachingStrategy="RecycleElement"
                Style="{StaticResource ProductsListStyle}">
                <ListView.Behaviors>
                    <behaviors:EventToCommandBehavior                    
                        EventName="ItemTapped"
                        Command="{Binding AddCatalogItemCommand}"
                        EventArgsConverter="{StaticResource ItemTappedEventArgsConverter}" />
                </ListView.Behaviors>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <templates:ProductTemplate />
                        </ViewCell>
                        <!--<ViewCell>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50*" />
                                    <ColumnDefinition Width="50*" />
                                </Grid.ColumnDefinitions>

                                --><!-- Left column - BindingContext set to ModelPair.Item1  --><!--
                                <templates:ProductTemplate Grid.Column="0" 
                                    BindingContext="{Binding Item1}" />

                                --><!-- Right column - BindingContext set to ModelPair.Item2 --><!--
                                <templates:ProductTemplate Grid.Column="1" 
                                    BindingContext="{Binding Item2}" />

                                --><!-- Don't show right column for "empty/invalid" models -->
                                <!--<v:ListViewColumnView.Triggers>
                                    <DataTrigger TargetType="v:ListViewColumnView"
                                             Binding="{Binding Id}"
                                             Value="-1">
                                        <Setter Property="IsVisible" Value="False"/>
                                    </DataTrigger>
                                </v:ListViewColumnView.Triggers>--><!--


                            </Grid>
                        </ViewCell>-->
                        <!--<Grid
                            ColumnSpacing="0"
                            RowSpacing="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition></ColumnDefinition>
                                <ColumnDefinition></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <ListView ItemsSource="{Binding Item1}" Grid.Column="1">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <ViewCell>
                                            <templates:ProductTemplate />
                                        </ViewCell>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                            <ListView ItemsSource="{Binding Item2}" Grid.Column="2">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <ViewCell>
                                            <templates:ProductTemplate />
                                        </ViewCell>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>-->
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <!-- INDICATOR -->
        <ActivityIndicator
            Grid.Row="0"
            Grid.RowSpan="2"
            Color="{StaticResource LightGreenColor}"
            IsRunning="{Binding IsBusy}"  
            IsVisible="{Binding IsBusy}"
            VerticalOptions="Center"
            HorizontalOptions="Center">
            <ActivityIndicator.WidthRequest>
                <OnPlatform x:TypeArguments="x:Double">
                    <On Platform="iOS, Android" Value="100" />
                    <On Platform="UWP, WinRT, WinPhone" Value="400" />
                </OnPlatform>
            </ActivityIndicator.WidthRequest>
        </ActivityIndicator>
    </Grid>
</ContentPage>