@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "Registo";
}

<h2 class="dama-heading-normal-font">@ViewData["Title"]</h2>
<h4>Associa a tua conta @Model.LoginProvider.</h4>


<p>
    Foste autenticado com sucesso no <strong>@Model.LoginProvider</strong>.<br />
    Por favor introduz um endereço de email para entrares na loja da Dama no Jornal&reg;<br /> e clica no botão Associar para finalizares o processo.
</p>

<div class="row">
    <div class="col-md-4">
        <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.Email"></label>
                <input asp-for="Input.Email" class="form-control" />
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-dama-menta">Associar</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

